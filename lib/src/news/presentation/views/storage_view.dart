import 'package:flutter/material.dart';

class StrorageView extends StatelessWidget {
  const StrorageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Storage View'),
    );
  }
}